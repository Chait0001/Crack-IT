# Production: relative path since frontend and backend share the same Vercel domain.
# This works for all Vercel deployments (production + preview URLs) without hardcoding.
# Override in the Vercel dashboard if the backend is on a separate domain.
VITE_API_URL=/api
